TODO
---

figure out why it takes so long to load
faster way to join JSON -- make it an array instead of an object? number the towns 0 - 250?

topojson vs geojson?

make better leaflet popups
include a spinning div (or something) as we wait for the map to load?

create map directives?